---
trigger: always_on
---

<rule>
After every update, push the code to it's related github repo. Always use two GitHub repositories. The main repository architecture should be built for local host running, and the V2 repository architecture should be built based on the hosting platform it will work in future to prevent issues in the migration process.
</rule>